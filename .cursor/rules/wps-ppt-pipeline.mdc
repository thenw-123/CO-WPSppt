---
description: 用户要做 PPT / 演示稿 / 幻灯片 / WPS 汇报时，走 OpenClaw wps-ppt 工具（COM 直驱 WPS）
alwaysApply: false
---

# WPS 演示自动化（OpenClaw + COM）

用户要做 **PPT / 演示 / WPS 汇报** 时：走 **JSON spec → `wps_dispatch` → COM**，不要默认 python-pptx 或 Markdown 假装幻灯片。整体数据流见仓库 [README.md](../README.md) 里的 **「核心逻辑」**。

## 项目位置

仓库根（与 `manifest.json`、`tools/` 同级）。

## Cursor 侧

1. [skills/drive-wps-ppt/SKILL.md](skills/drive-wps-ppt/SKILL.md)；答辩类还须 Skill 里的生成与视觉 QC 约定。
2. **已接 Gateway MCP**：`openclaw_discover` 有 **`wps-ppt`** → `openclaw_invoke`（`action` + `args_json`）。无工具则网关加载 [manifest.json](manifest.json)，或改 [tools/wps_dispatch.ps1](tools/wps_dispatch.ps1)。详见 [docs/MCP-OpenClaw与wps-ppt.md](docs/MCP-OpenClaw与wps-ppt.md)。
3. 成稿优先 **`run-spec`**（`specPath` 或内联 `spec`）；叙事版式见 `specs/narrative-layouts-demo.json`。默认校验；大稿可先 `validate-spec`；环境用 `doctor`。
4. 边做边改：`new` → `add-slide` / `set-text` / `set-notes` → `save`。

## 参数约定

- 所有脚本从 stdout 输出一行 JSON：`{"ok":true,"data":{...}}` 或 `{"ok":false,"error":"...","code":"..."}`；失败时若有 `data.errors`（校验）一并交给用户。
- 可通过环境变量 `OC_ARGS_JSON` 传入与 `args_json` 相同的 JSON 字符串。
- 会话文件：`logs/wps-session.json`（当前绑定的 `presentationPath`）。

## 安全

- 不要用 `exec` 跑不可信脚本；`exec` 仅允许项目内的 `.ps1` 路径。
